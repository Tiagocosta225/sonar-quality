name: CI/CD Node.js + SonarQube

on:
  push:
    branches: [main, develop]
  pull_request:
    types: [opened, synchronize]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
    - name: Install & Test
      run: |
        cd node-app
        npm ci
        npm test -- --coverage reporters=lcov

  sonarcloud:
    needs: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Shallow clones should be disabled for a better branch coverage
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@v3
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        args: >
          -Dsonar.organization=tiago-org
          -Dsonar.projectKey=node-app-demo
          -Dsonar.sources=node-app/src
          -Dsonar.tests=node-app/tests
          -Dsonar.javascript.lcov.reportPaths=node-app/coverage/lcov.info

  quality-gate:
    needs: sonarcloud
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
    - uses: sonarsource/sonarqube-quality-gate-action@master
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        SONAR_HOST_URL: https://sonarcloud.io
